rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras permissivas para desenvolvimento - ALTERE PARA PRODUÇÃO
    match /{document=**} {
      allow read, write, create, update, delete: if true;
    }
  }
}